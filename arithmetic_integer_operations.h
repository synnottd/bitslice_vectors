void add(
	unsigned * first,
	unsigned * second,
	int size,
	unsigned * result);

void multiply(
	unsigned * first,
	unsigned * second,
	int size,
	unsigned * result);

void mapAnd(
	unsigned * array,
	unsigned * result,
	unsigned operand,
	int size);